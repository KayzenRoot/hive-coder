# GEF V1 Correction Prompt — Hive Coder

Apply the approved Correction Delta for `<WORK_ORDER_ID>` in the SAME branch/PR.

Freeze all accepted findings and decisions. Address only the declared open finding/root cause. Respect target files/symbols, budgets and forbidden shortcuts. Re-run invalidated proofs plus required regression checks. Update exact-head Evidence Bundle. Stop after the corrected candidate is published; do not start new work.
