# Proposed Checkpoint Delta — HCODER-WO-0003

**Status:** PROPOSED · do not treat as canonical until HEDS approval.

If approved, promote only these truths:
- Hive-owned shell-free child-process lifecycle exists for stdio foundations.
- Hive-owned strict NDJSON JSON-RPC 2.0 peer exists with bounded request timeout and fail-closed protocol handling.
- Production constructors perform exact pinned binary-version preflight before launching ACP/MCP mode.
- Open Interpreter ACP v1 initialize, session creation, session cancellation notification, session update collection and session close are implemented and mock-contract tested without executing a model prompt.
- Cua Driver modern MCP `2026-07-28` `server/discover` is implemented and mock-contract tested.
- The Cua adapter exposes discovery only. It has no `tools/call` API and therefore cannot execute desktop actions in this increment.
- Unsupported inbound JSON-RPC requests are denied by default.

Do not promote any claim that real Open Interpreter prompting, Cua desktop actions, permission mediation, UI, packaging or production deployment is proven.
