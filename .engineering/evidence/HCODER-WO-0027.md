# HCODER-WO-0027 — Evidence Bundle

**Status:** CORRECTION ROUND APPLIED — FOUR REVIEW FINDINGS FIXED LOCALLY, LANES RE-MEASURED ON THE NEW HEAD, CARRIED ON THE SAME DRAFT PR, NOT APPROVED, NOT MERGED
**Issue:** #89
**Slice:** HCODER-DIST-001D
**Decision:** `DEC-031` — PROPOSED / NOT CANONICAL. Nothing in this bundle promotes it.
**Canonical base at prebuild:** `5d97df6cf2f69a35d14af140e28088a7659eff7a`
**Exact source for this run:** `a9b48bce43fcc2c1a14b70036ed4555f52ba3537` — `origin/main` after `HCODER-WO-0027: correct executor base after CR-001`, verified by `git fetch origin --prune` before any edit, which is the handoff's stop-before-editing condition. It is not triggered, so implementation continues on that source.
**Reviewed head this round corrects:** `576a40f105a831fe6916c4c3fa8710445a3bf952` — verdict `CORRECTION_REQUIRED`, `CRITICAL 1 / HIGH 1 / MEDIUM 2 / LOW 0`, PR `#93` kept Draft. See *Correction round*.

> **What this bundle is.** A record of what the repository now contains and of what was proven on one Windows host. No sentence here is evidence that an update endpoint, a signing key pair, a publisher, a certificate or a runner exists. Hosted receipts are not duplicated into this file at all, following the `HCODER-WO-0026` rule that a mutable run identifier inside a frozen document is how a bundle starts lying after its head moves: the four workflow runs and their per-job status are recorded in the Draft PR thread and in Issue `#89`, each bound to the exact head they measured.

## Prebuild facts
- CP-0026 / DEC-030 canonical and sealed.
- PR #88 source-truth reconciliation merged and post-validated before this Work Order began.
- Current UpdateService is inert.
- No updater plugin dependency or JS guest binding exists at prebuild.
- desktop capability permissions are empty.
- bundle.active is false.
- no updater signing key/public production trust configuration is claimed.

## Evidence slots for executor
- **start SHA:** `a9b48bce43fcc2c1a14b70036ed4555f52ba3537` for the reconciled branch. The work itself began one commit earlier, on `e5cfe3267f968d80cc3c4dc91cb6acf54ee7eb80` (Delta-001 `#91` merged); see *Source-truth reconciliation onto Delta-002* for how the earlier head was preserved rather than redone.
- **final SHA:** not asserted here. This bundle is carried by the branch tip, and a SHA written into a frozen document goes stale the moment a commit follows it. The candidate head, the four hosted runs measured against it and the review state are recorded in the Draft PR and Issue `#89`, each bound to the exact head it measured.
- **changed files:** 14 at this head — new `apps/desktop/src-tauri/src/update_admission.rs` and this bundle; modified `apps/desktop/src-tauri/{Cargo.toml,Cargo.lock,src/lib.rs,tauri.conf.json}`, `apps/desktop/src/{contracts/updateState.ts,contracts/updateState.test.ts,lib/desktopBridge.ts,lib/updateService.ts,lib/updateService.test.ts}`, `tools/desktop/security_gate.py`, and since the correction round `apps/desktop/{package.json,package-lock.json}`. Nothing under `docs/project-brain/`, no workflow, no capability file. `apps/desktop/src-tauri/gen/` and `src-tauri/icons/` are generated locally and are deliberately untracked and unstaged.
- **selected updater crate:** `tauri-plugin-updater = "=2.12.0"` (exact pin, default features `rustls-tls`, `system-proxy`, `zip` retained).
- **selected Tauri CLI:** **moved to `2.11.5`** in the correction round — `@tauri-apps/cli` `2.11.4 → 2.11.5` in `apps/desktop/package.json` with `package-lock.json` regenerated by `npm install --package-lock-only` and verified by a clean `npm ci`; `tauri` crate stays `=2.11.5`, `tauri-build` stays `=2.6.3`. The move is the CRITICAL finding's correction and is exactly what the Context Lock's conditional admission of these two files covers (*The signed-version gap* explains why the earlier "blocker" framing was wrong). `MIN_TAURI_CLI_VERSION = (2, 11, 5)` in the gate now holds the floor, with manifest/lock parity checked both ways. No workflow needed touching: the four hosted lanes read the CLI version from the installed package (`require('@tauri-apps/cli/package.json').version`) rather than pinning it themselves, so the manifest is the only live source of the pin. The `2.11.4` mentions that remain in `HCODER-WO-0024/0025/0026` evidence and prebuild files are records of their own heads and are not rewritten.
- **upstream signed-version source:** `tauri-apps/tauri` `crates/tauri-cli/src/helpers/updater_signature.rs` — present with the `\tversion:` append at tag `@tauri-apps/cli-v2.11.5`, absent at `@tauri-apps/cli-v2.11.4`; consumer is `tauri-apps/plugins-workspace` `plugins/updater/src/config.rs` at tag `updater-v2.12.0` (`require_signed_version` line 135, `allow_downgrades` line 148) and `src/updater.rs` `verify_signature` → `verify_signed_version` → `signed_version` (lines ~1529–1606).
- **trust config model:** `UpdaterTrustProbe` — camelCase, `#[serde(deny_unknown_fields)]`, **seven required** fields (`pubkey`, `endpoints`, `requireSignedVersion`, `allowDowngrades`, three `dangerous*`). A missing field is refused, not defaulted, so the upstream `#[serde(default)]` posture cannot silently downgrade this client. An extra field is refused too, which includes upstream's own `windows` sub-node: the Delta-001 node shape is the only admitted trust shape, and a slice that needs `windows.installerTypes` must say so in a new delta rather than be tolerated here.
- **new command set:** `get_update_status`, `check_for_update`, `download_update_candidate` — three, argument-free, registered in `generate_handler![]` and reachable only through three named bindings in `desktopBridge.ts`.
- **capability diff:** none. `capabilities/desktop-read-only.json` is byte-identical to its prebuild content with `permissions: []`.
- **lockfile diff:** `Cargo.lock` 432 → 483 packages (+51, `tauri-plugin-updater` and its `reqwest`/`rustls`/`minisign`/`zip` graph plus the four direct pins `semver =1.0.28`, `serde_json =1.0.151`, `sha2 =0.10.9`, `tauri-plugin-updater =2.12.0`). `apps/desktop/package-lock.json` changed only in the correction round: 48 added / 48 removed lines, every one of them the `@tauri-apps/cli` entry or one of its 11 per-platform `@tauri-apps/cli-*` binary packages moving `2.11.4 → 2.11.5`. No other JS package, no dependency added or removed, no `node_modules` committed.
- **focused tests:** Rust `cargo test --locked` **42 passed / 0 failed**, of which 29 are `update_admission` cases over the 13 the crate had at the base (20 at the reviewed head, +9 in the correction round). Desktop `vitest run` **129 passed / 0 failed** across 9 files; `contracts/updateState.test.ts` went 39 → 40 cases and `lib/updateService.test.ts` 13 → 28, so +16 with no case deleted — several existing cases were retargeted to the DEC-031 law, and each rewrite is described in *Contract law added* below.
- **security gate:** `DESKTOP_SECURITY_GATE=PASS` with `TAURI_COMMANDS=check_for_update,choose_workspace,download_update_candidate,get_desktop_snapshot,get_runtime_status_envelope,get_update_status`, `FRONTEND_INVOKES=6`, `CAPABILITY_PERMISSIONS=0`, `UPDATE_COMMAND_ARGS=0`, `UPDATE_RAW_DECODER=STRICT`, `GUEST_UPDATER_PERMISSIONS=0`, `UPDATER_TRUST_CONFIG=DELTA_001_FAIL_CLOSED`, `UPDATER_PLUGIN_PIN=2.12.0`, `TAURI_CLI_PIN=2.11.5`, `INSTALL_RESTART_AUTHORITY=0`, `FILESYSTEM_MUTATION_PRIMITIVES=0`, `GENERIC_PROCESS_EXECUTION=0`, `LOCKFILES=COMMITTED`.
- **version drift:** `VERSION_DRIFT=LOCKED`, canonical source `apps/desktop/src-tauri/tauri.conf.json:0.1.0`, mirrors `Cargo.toml:0.1.0` and `package.json:0.1.0`.
- **Rust tests/check:** `cargo check --locked --all-targets` clean, zero warnings — and that is the command the zero-warning claim rests on. `cargo test --locked` additionally emits one MSVC linker line (`Criando biblioteca …` / `link.exe` stdout) on this host while building the test binary; it is the toolchain announcing an output file, not a compiler diagnostic, and it is not counted as a warning either way. `cargo audit --no-fetch` exit 0 with **7 allowed warnings**, and the same command against `git show a9b48bc:apps/desktop/src-tauri/Cargo.lock` reports the **same 7** (`glib` unsound, `proc-macro-error` + five `unic-*` unmaintained) — so the +51 packages add no finding.
- **web tests/typecheck/build:** `npm run typecheck` clean; `npx vitest run` 129/129; `npm run build:web` builds.
- **npm audit:** `found 0 vulnerabilities`.
- **Governance run:** measured against the exact pushed head and reported in the Draft PR, never restated here — see *Proposed checkpoint delta* item 4.
- **Desktop Shell run:** same placement rule as Governance.
- **Native Package Matrix run:** same placement rule. This is the head that closes U16's hosted half.
- **Protected Release run:** same placement rule, and it is in any case structurally capped: `bundle.active` is false, no signing or publication authority exists, so its credential-bearing stages stay `UNKNOWN` / `BLOCKED` and are not reported as passing on any head.
- **blockers:** no trust material exists by design, so no real check or download has ever executed and the *demonstration* half of U6 stays open (the *satisfiability* half is closed at this head — see *The signed-version gap*); `bounded_snapshot`'s config probe has no unit test, only a directly-tested primitive; the local `tests/runtime` git-stage lane cannot run here (see *Local lanes that did not run*); HIVE is not serving on this host.
- **HIVE refresh:** NOT AVAILABLE — re-probed for this round rather than assumed. The Docker daemon answers (`29.7.2`), but nothing is running: `curl http://127.0.0.1:8000/api/v1/health` returns `http=000`, `docker ps` is empty, and every `hive-v102-*` and `hive-wo031-*` container is `Exited`. Bringing another workspace's stack up to obtain a card is not this Work Order's authority, so no claim in this bundle rests on a HIVE card and none is simulated. Recorded as the real blocker the handoff asks for, not as a step that passed.
- **proposed checkpoint delta:** see *Proposed checkpoint delta* at the end of this file. Nothing is minted here.

## Source-truth reconciliation onto Delta-002

Implementation of this Work Order ran in two pieces, and the seam is recorded rather than smoothed over.

- The code was written on `e5cfe3267f968d80cc3c4dc91cb6acf54ee7eb80`, the head Delta-001 produced, and was committed there. That is the head every local gate in *Verification executed locally* was first measured against.
- `a9b48bce43fcc2c1a14b70036ed4555f52ba3537` then became `origin/main`: `HCODER-WO-0027: correct executor base after CR-001`, merging PR `#92`. Its whole diff is `.engineering/context-locks/HCODER-WO-0027.md` and `.engineering/prebuilt/HCODER-WO-0027-EXECUTOR-BRIEF.md` — verified with `git diff --name-only e5cfe32 a9b48bc`. Neither path is one this implementation touches, and neither carries a product, runtime, trust, dependency, workflow or test-contract change, so Delta-002 grants no new authority and unlocks nothing beyond what Delta-001 already did.
- Reconciliation moved the branch instead of redoing it: local `main` was fast-forwarded to the verified `a9b48bc` and the feature branch rebased onto it. No reset, clean, discard, force-push or history rewrite was used, and no local work was dropped. Reading Delta-002 *before* editing is what the handoff requires, because an executor that starts from a superseded source cannot know which lock delta governs it.
- **Every gate was then re-measured on the rebased head** (see *Verification executed locally*). A commit is not evidence, but it does invalidate receipts: the pre-rebase numbers below are reproduced from the post-rebase tree, not carried forward.

The practical consequence for the reviewer is that the candidate's ancestry contains both the Delta-001 trust-node unlock and the Delta-002 source correction, so the Context Lock it must be read against is the one at its own tip.

## What the repository now contains

Source-materialized claims — true of the tree, independent of any runner:

- `apps/desktop/src-tauri/src/update_admission.rs` is the only place in this build that can hold updater state. It resolves trust from this build's own configuration, evaluates the DEC-028 channel/version law against what an endpoint announced, and keeps one pending slot (`Empty | Admitted | InFlight | Verified | Refused`) behind a mutex, with the official `Update` handle stored beside the identity it was admitted for and, once verified, the bytes that identity hashes stored in the same place. It performs no install, restart, publication or filesystem mutation; two `const _: fn(&tauri_plugin_updater::Config) -> bool` items make the 2.12 field surface a compile-time dependency rather than a hope.
- The slot has a small law of its own, because holding a payload made its loss observable. A refusal that only says "the slot is occupied" cannot overwrite the candidate it was refused against; a genuine fault releases a pending or in-flight candidate so the next check is a real retry; a `Refused` slot is the only non-empty state a later `begin()` may claim; a `Verified` candidate is never demoted; and a completion may bind only to the `metadata_sha256` the slot already owns. So at most one candidate is ever live, it cannot be silently replaced, and it cannot be destroyed by an unrelated read.
- Three commands reach the guest. Each is argument-free; each answers with a `hive-update-state-v1` snapshot built from a closed `WireStatus` of exactly the eight contract keys, so a remote body, URL, signature, public key or header has no field to arrive in — and neither can a payload, which is why the retained bytes have no route out. Upstream error messages are classified into a 16-name refusal vocabulary and discarded, because they quote endpoints.
- `get_update_status` on an empty slot is not a free `idle`. The status read first resolves this build's own trust configuration and, if it does not resolve, answers with the refusal instead: `idle` is a state the bridge grants only to a build that has a trust root. This is a reading of local configuration and performs no request, and it is what lets the frontend distinguish "an updater exists" from "the bridge answered" without inventing a wire field for it.
- `apps/desktop/src/lib/desktopBridge.ts` remains the only file that calls `invoke`, now with 6 sites and no generic helper: a shared `invoke(command)` function would have turned three named bindings back into one caller-selectable surface, so each of the three repeats its own literal command constant.
- `BridgedUpdateService` caches only what `evaluateStatus` accepted, and additionally refuses a snapshot whose `currentVersion`/`channel` disagree with this client's own identity. Any fault — throw, malformed wire, identity disagreement — replaces the cached claim with an honest `unavailable` snapshot rather than keeping a stale one. Its `availability()` now separates the two facts it used to merge: reachability is what the client observed (`bridge_unreachable`), usability is what the snapshot can evidence (`not_configured` vs `configured`), and the third reason is only ever returned by a build whose own status read was answered by a resolved trust root.
- The frontend never sees the trust root: `tauri.conf.json → plugins.updater` is the Delta-001 fail-closed node (`pubkey: ""`, `endpoints: []`, `requireSignedVersion: true`, `allowDowngrades: false`, three `dangerous*` false), and the security gate now fails if that node drifts in either direction.

## The signed-version gap (DEC-031's loadable premise, checked rather than assumed)

`requireSignedVersion: true` is only meaningful if the signature's minisign trusted comment carries a `version:` field. Three facts, each reproducible on this host:

1. The plugin reads that field and refuses without it. `tauri-plugin-updater` 2.12.0 `src/updater.rs`: `signed_version()` splits the trusted comment on tabs and looks for a `version:` prefix; when it finds none and `require_signed_version` is set, `verify_signed_version()` returns `Error::MissingSignedVersion`.
2. Only CLI ≥ 2.11.5 writes it. `crates/tauri-cli/src/helpers/updater_signature.rs` appends `\tversion:{version}` at tag `@tauri-apps/cli-v2.11.5`; at `@tauri-apps/cli-v2.11.4` the file builds the comment as `format!("timestamp:{}\tfile:{}", …)` with no version field at all, and the 2.11.5 file adds both the guard panic (`"the app version {version:?} cannot be signed because it contains a tab or newline"`) and the upstream test `embeds_version_in_trusted_comment`. **The pin here is now 2.11.5**, so the toolchain in this tree is the one that can write the field. Two limits on that sentence, stated rather than smoothed: the installed 2.11.5 binary does carry the version-only guard string, but a byte scan of the shipped `cli.win32-x64-msvc.node` did not locate the `\tversion:` literal itself, so the append is proven from the tagged source and not from the artifact; and no command this slice is allowed to run can produce a signed artifact at all, because `createUpdaterArtifacts` stays disabled by stop-condition and no private key exists. The end-to-end demonstration therefore remains unproduced, and is not claimed.
3. 2.11.x of the plugin cannot honour the config at all. `plugins/updater/src/config.rs` at tag `updater-v2.11.0` has neither `require_signed_version` nor `allow_downgrades`, and the struct carries no `deny_unknown_fields`, so a `requireSignedVersion` key would be parsed away and silently ignored. `MIN_UPDATER_PLUGIN_VERSION = (2, 12, 0)` in the gate exists because a floor is the only durable answer to a key that fails open. The same reasoning now applies to the CLI: `MIN_TAURI_CLI_VERSION = (2, 11, 5)` holds it, and the gate fails on the manifest pin, on a lock that resolves differently, and on a version string it cannot parse.

**Consequence, restated after the correction.** The blocker this section recorded at the reviewed head is closed at this head: with `@tauri-apps/cli` 2.11.5 pinned and gate-held, an artifact this repository builds *can* carry the signed version that the admitted `requireSignedVersion: true` posture demands. What remains unproven is a different thing and stays unproven — that a *successful* verified download happens. It cannot be demonstrated here for two independent reasons that are both intentional: the trust data is empty, so `resolve_trust` refuses before any request; and no signing key, endpoint or artifact production is authorized by this slice. U6 is therefore "the requirement is satisfiable and the refusal path is tested", not "an update was verified".

**What the earlier framing got wrong.** This section previously asserted the pin could not move because "neither file is an admitted implementation path under this Work Order's Context Lock". That was false, and it is corrected rather than reinterpreted: Delta-002's lock admits `apps/desktop/package.json` — "only exact Tauri CLI alignment if source check requires" — and `apps/desktop/package-lock.json` as "lock mirror of the same". The source check did require it, which is exactly the condition the admission was written for, so the move happened inside the existing lock and no new delta was needed. A blocker that the governing document already provides for is not a blocker, and calling it one risked a successor slice spending a governed unlock on a door that was open.

## Deviations from the Implementation Pack

None at this head, and the previous entry here is retracted rather than left standing.

That section recorded that pack step 8 ("store exact update handle + verified bytes in memory") was deliberately not followed: `admit_verified_bytes()` hashed the payload and dropped it, justified by the absence of install authority. The review read it as what it was — a spec violation dressed as a decision, and the HIGH finding of this round. Dropping the bytes does not make `ready` less of a claim; it makes the claim unfunded, because R4's own sentence is what makes the single pending candidate *the thing a later governed install consumes*. An artifact that has to be re-downloaded and re-verified before it can be installed is not a ready candidate, it is a suggestion.

The pack is now followed as written. `VerifiedCandidate` owns `bytes: Vec<u8>` alongside the admitted identity and the proof derived from those exact bytes; `Held::Verified` is the only place they live; nothing writes them to a file, no wire field can carry them, and the field is explicitly `#[allow(dead_code)]` with the reason attached rather than deleted, because reading them out is the separately governed install slice. Retention adds no capability: the three commands, the closed wire and the `bundle.active: false` posture are unchanged.

Two properties had to be settled at the same time, because holding bytes makes them observable: a second completion can no longer rebind the ready candidate's payload (`a_second_completion_cannot_replace_the_ready_candidates_bytes`), and a later refusal — duplicate or genuine — can no longer demote a verified candidate and lose the bytes it verified (`a_verified_candidate_survives_every_later_refusal`).

## Correction round against `576a40f`

Verdict `CORRECTION_REQUIRED`, `CRITICAL 1 / HIGH 1 / MEDIUM 2 / LOW 0`, carried on the same Work Order and the same Draft PR, which stays Draft. Each finding is closed by code plus a guard that was shown to fail against a mutation of the very thing it now protects; none is closed by a paragraph.

| Finding | What was actually wrong | What the tree does now | Proof |
| --- | --- | --- | --- |
| CRITICAL — U6 signed-version binding unsatisfiable | `@tauri-apps/cli` was pinned at 2.11.4, which signs `timestamp:`/`file:` only, so no artifact this repository could build would ever carry the version the admitted `requireSignedVersion: true` demands. The bundle then called the pin unmovable on a reading of the Context Lock that was simply false. | CLI aligned to `2.11.5` in `package.json`, lock regenerated and re-verified with a clean `npm ci`, and the floor written into the gate as `MIN_TAURI_CLI_VERSION = (2, 11, 5)` with manifest/lock parity and unparsable-version checks. | `TAURI_CLI_PIN=2.11.5` on `PASS`; the pin-downgrade mutation fails the gate; and the corrected gate run against the reviewed head's **own** `apps/desktop` tree reports `DESKTOP_SECURITY_GATE=FAIL` with exactly one refusal — the floor line. That last run is the finding closing itself: the head that was reviewed is the head the new gate rejects. |
| HIGH — U12 verified bytes dropped | R4 says the single pending candidate holds the verified bytes; the implementation hashed them and discarded them, and the bundle documented that as a deliberate deviation. | `VerifiedCandidate` retains `bytes`, bound to the same `metadata_sha256` its proof carries; still no file write, still no wire field. | `verified_candidate_retains_exactly_the_bytes_it_hashed`, `ready_status_carries_a_binding_and_never_the_payload`, `a_second_completion_cannot_replace_the_ready_candidates_bytes`, `completion_only_binds_the_candidate_the_slot_owns`. |
| MEDIUM — a refusal could destroy the candidate it was refused against, and nothing could retry | `record()` overwrote whatever the slot held, so a duplicate `check_for_update` threw away the admitted candidate *and* its `Update` handle; and `begin()` accepted only `Held::Empty`, so once anything was refused the bridge was wedged for the life of the process. | Slot-occupancy refusals (`candidate_already_admitted`, `no_admitted_candidate`) are inert against a slot that already owns a candidate; a genuine fault releases a pending or in-flight slot, and a refusal is the one state a later check may lawfully claim. `Held::Verified` is never demoted. `take_for_download` now reports the in-flight slot as occupied rather than empty. | `a_duplicate_check_leaves_the_pending_candidate_held`, `a_second_download_attempt_leaves_the_in_flight_candidate_alone`, `a_verified_candidate_survives_every_later_refusal`, `a_failed_download_releases_the_slot_so_a_later_check_is_a_real_retry`, `an_unreachable_service_is_reported_until_a_retrying_check_replaces_it`. |
| MEDIUM — `availability()` reported an unconfigured updater as usable | `{ available: true, reason: "not_configured" }` was the fallback for any reachable bridge, and a fresh backend that had never resolved a trust root answered `idle`, so the shipped keyless build presented itself as a usable updater. It also conflated two different facts: the bridge answered, and an updater exists. | Split. Reachability is what the client observed; usability is what the snapshot can evidence — `unavailable` means not configured, and every remaining state is one the bridge only grants after its own trust root resolved. The Rust side earned that inference: `get_update_status` now refuses to report `idle` for a build whose configuration does not resolve, which is a reading of local config and still performs no request. | `reports a reachable bridge and a configured updater as separate facts`, `reports the shipped build as reachable and unusable on its first read`, `says so when it has never reached its backend`. Deleting the configuration branch turns the first red and deleting the reachability branch turns the third red, so neither half of the split rests on the other. |

The last row carries the one judgement call worth naming for the reviewer. `availability()` is a property of the frontend, but the fact it reports lives in trusted Rust state, and no field of the closed `hive-update-state-v1` wire describes configuration. The alternative to making the status read honest was inventing a wire key for it, which the contract's closed key set exists to prevent. So `idle` is now a claim the bridge has to earn — a build with no trust root answers `unavailable` to a plain status read, and a build with one answers `idle`. That is a behaviour change to a command that already existed, made inside the same Work Order because it is the only shape in which the frontend finding can be closed without widening a contract.

Two limits that this round did not remove:

- `bounded_snapshot`'s configuration probe is not unit-covered, because `get_update_status` needs a live `AppHandle`. The primitive it turns on, `UpdateAdmissionBridge::holds_nothing`, is covered directly, and mutating it to `Ok(true)` fails that test — but the wiring from probe to reported state is verified by reading, and by the security gate's command set, not by a Rust test.
- The CRITICAL's end-to-end half stays open for the reason in *The signed-version gap*: producing a signed artifact requires `createUpdaterArtifacts`, which this slice forbids, and a private key, which does not exist here. Aligning the toolchain is what the finding asked for and what was done; demonstrating a verified download is still not available to any build in this repository.

## Contract law added while making that gap honest

Admitting a proof scheme made `ready` producible, which exposed an asymmetry the pre-DEC-031 law had handled with one gate: a caller presenting a well-formed proof could reach `installing`. Install readiness and install progress are different claims — the first says a verified artifact was obtained, the second says the running product was mutated — so they are now gated separately:

- `PROOF_GATED_TRANSITIONS` shrank to `verifying → ready` only.
- `evaluateTransition` refuses any edge whose destination is `installing` or `success` with the new `install_authority_not_governed` reason, **before** the proof gate, whatever evidence accompanies it.
- `evaluateStatus` refuses an install-progress snapshot on its face, and `evaluatePersistedEvent` refuses both an install-progress source and an install-progress outcome.
- Fixing this surfaced a real defect: `evaluateStatus` passed its internal validated record into the public `evaluateAuthenticityProof`, which re-wrapped it and read `data("scheme")` off the wrapper, so **a legitimate `ready` snapshot could never have been admitted**. It now calls a record-level internal, and the public entry point keeps its untrusted-input contract.
- The pack's TS list required the exact admitted proof shape, so `AUTHENTICITY_PROOF_KEYS` is exported and a proof carrying any extra key is refused — unreviewed material cannot ride along with a nominally valid proof.

When HCODER-DIST-001E governs install authority, `ready → installing` and `installing → success` must re-enter the proof-gated set; that obligation is written into the `PROOF_GATED_TRANSITIONS` doc comment, not just here.

## Verification executed locally

Every row was measured on the corrected head this bundle now carries — after the `576a40f` review round and on the same tree this file is committed with. An earlier commit supersedes a receipt, it does not extend it, so none of these numbers is carried forward from the pre-correction head. Hosted lanes are absent by construction here, so nothing below is claimed as exact-head CI.

| What | Exact command | Result |
| --- | --- | --- |
| Rust admission + shell lane | `cargo test --locked` | `42 passed; 0 failed` in the lib target, `0 failed` in the two remaining targets (plus one MSVC linker output notice, not a diagnostic) |
| Rust build, all targets | `cargo check --locked --all-targets` | `Finished` in 12.15 s, no warnings |
| Rust advisories, this lock | `cargo audit --no-fetch` | exit 0, 483 crate dependencies, **7 allowed warnings** |
| Rust advisories, base lock | `cargo audit --no-fetch -f <git show a9b48bc:…/Cargo.lock>` | exit 0, 432 crate dependencies, the **same 7** warnings — so the +51 packages add no finding |
| Desktop unit lane | `npx vitest run` | `129 passed (9 files)` |
| Desktop types | `npm run typecheck` | `tsc --noEmit`, exit 0, no diagnostics |
| Web bundle | `npm run build:web` | built — `dist/assets/index-n0FwC3m1.js 237.52 kB │ gzip 74.03 kB` |
| JS advisories | `npm audit` | `found 0 vulnerabilities` |
| Governance gate | `python tools/desktop/security_gate.py` | `DESKTOP_SECURITY_GATE=PASS`, including `TAURI_CLI_PIN=2.11.5` |
| Version lock | `python tools/desktop/version_drift.py` | `VERSION_DRIFT=LOCKED` |
| Python lanes not touched by this WO | `python -m pytest tests/desktop tests/control_plane tests/foundations -q` | `334 passed` |
| Runtime lane (environment-blocked) | `python -m pytest tests/runtime -q` | `39 failed, 364 passed, 72 skipped` — the dulwich gap below, not a regression |
| Rust formatting | `cargo fmt --all -- --check` | **FAILS**, and fails on the canonical base as well — see the note below |

**The formatting row is a refusal, not a pass, and it is not new.** `cargo fmt --check` reports 17 diff sites at the canonical base `a9b48bc` (14 in `lib.rs`, 3 in `runtime_status_supervisor.rs`) and 68 at this head (47 in `update_admission.rs`, 18 in `lib.rs`, the same 3 untouched). This crate has never been rustfmt-default, and no gate in this repository runs it. Making this branch green would mean reformatting inherited files for aesthetics — the brownfield preservation rule and the handoff's "smallest safe delta" both point the other way, so the delta deliberately leaves the style alone and records the measurement instead. Formatting `update_admission.rs` is a purely cosmetic commit that a reviewer may ask for separately.

## Local lanes that did not run, and why

`python -m pytest tests/runtime -q` on this host is `39 failed, 364 passed, 72 skipped`, re-measured on this head. All 39 failures are in `tests/runtime/test_git_stage_*` and every one of them is the WO-0023 provenance gate refusing to proceed because the admitted pure-Python backend is absent: the traceback reaches `distribution("dulwich")` at `hive_runtime/git_stage_index_codec.py:146` and raises for the missing distribution, with the install command named in the exception text. They do not import anything this Work Order changed, and no file under `hive_runtime/` or `tests/runtime/` is modified. Reported as an environment gap on this host, not as a pass and not as a regression.

The whole-repository lane, `python -m pytest tests -q`, is therefore `39 failed, 698 passed, 72 skipped` here — the same 39 and nothing else, with the 698 being the 364 above plus the 334 from the three lanes this Work Order can actually measure. Installing `dulwich` to turn those rows green is not this branch's call: it changes the host's dependency set rather than the repository's, and the failing lane is the governed Git-staging substrate sealed under `HCODER-CP-0023`.

No hosted lane ran either. The four workflows measure the pushed head, and every receipt for a previous head stops being evidence the moment this file is committed.

## Non-vacuity proof

A green gate proves nothing if it cannot fail. Twenty-two single-line mutations were applied to clean mirrors built from the working tree this bundle is committed with — the gate and Rust mirrors outside the repository, the JS mirror necessarily inside `apps/desktop` so `vitest` resolves `node_modules`, and removed by the run itself. Three controls measured `PASS` before any mutation: the unmutated gate mirror, the unmutated Rust mirror at 42 tests, and the unmutated JS mirror at 28. Each case was expected to be caught by the one guard named in it, and the row below records that guard's own first refusal line or first panicking assertion, copied from the run rather than paraphrased.

| Mutation | Guard verdict |
| --- | --- |
| **Security gate — the ten guards that already carried the reviewed head** | |
| pin `tauri-plugin-updater` down to `=2.11.2` | DETECTED — `tauri-plugin-updater must be at least 2.12.0: older pins ignore the signed-version and downgrade keys` |
| `"requireSignedVersion": false` | DETECTED — `updater plugin node must equal the Delta-001 fail-closed posture, drifted: ['requireSignedVersion']` |
| non-empty `"pubkey"` | DETECTED — same node equality, `drifted: ['pubkey']` |
| `install_update` registered in `generate_handler![]` | DETECTED — `forbidden production install command: install_update` |
| `invoke(UPDATE_CHECK_COMMAND, args)` | DETECTED — `invoke site must name a declared constant and carry no arguments` |
| generic `invoke(command)` helper | DETECTED — `expected exactly 6 frontend invoke() calls, found 5` |
| capability `"permissions": ["updater:default"]` | DETECTED — `desktop capability permissions must be empty, got ['updater:default']` |
| `update.install(...)` in production Rust | DETECTED — `forbidden production updater install call: .install(` |
| trust keys named in frontend `.ts` | DETECTED — `forbidden updater trust config in frontend source: requireSignedVersion` |
| drop `deny_unknown_fields` from the trust probe | DETECTED — `updater admission guard missing` |
| **Security gate — the guard this round added** | |
| `@tauri-apps/cli` pinned back to `2.11.4` | DETECTED — `@tauri-apps/cli must be at least 2.11.5: older CLIs sign updater artifacts without a version field, which makes the admitted requireSignedVersion posture unsatisfiable` |
| **Rust lane — U12 byte custody** | |
| `bytes: bytes.to_vec()` → `bytes: Vec::new()` (the exact pre-correction behaviour) | DETECTED — `verified_candidate_retains_exactly_the_bytes_it_hashed` panics |
| the verified payload pushed onto the status wire | DETECTED — `ready_status_carries_a_binding_and_never_the_payload` panics |
| a second completion allowed to rebind the ready slot | DETECTED — `a_second_completion_cannot_replace_the_ready_candidates_bytes` panics |
| **Rust lane — slot ownership, refusal and retry** | |
| completion accepted for a candidate the slot does not own | DETECTED — `completion_only_binds_the_candidate_the_slot_owns` panics |
| slot-occupancy refusals allowed to overwrite the held candidate | DETECTED — `a_duplicate_check_leaves_the_pending_candidate_held` panics |
| the `Held::Verified` guard made to match the wrong variant | DETECTED — `a_verified_candidate_survives_every_later_refusal` panics |
| `begin()` restricted to an empty slot only (no retry after a refusal) | DETECTED — `a_failed_download_releases_the_slot_so_a_later_check_is_a_real_retry` panics |
| an in-flight slot reporting `no_admitted_candidate` instead of occupied | DETECTED — `a_second_download_attempt_leaves_the_in_flight_candidate_alone` panics |
| `holds_nothing()` made to answer `Ok(true)` unconditionally | DETECTED — `an_unreachable_service_is_reported_until_a_retrying_check_replaces_it` panics |
| **Frontend lane — availability** | |
| delete the branch that reports `unavailable` as not configured | DETECTED — `BridgedUpdateService > reports a reachable bridge and a configured updater as separate facts` fails |
| delete the branch that reports an unreachable bridge | DETECTED — `BridgedUpdateService > says so when it has never reached its backend` fails |

Result: **22 detected / 22 applied, 3 controls PASS, `unexpected=0`, driver exit 0**, and `git status --porcelain` afterwards showing exactly the eight files this round touched plus the two generated directories that stay untracked. Each Rust row above is the first panicking assertion the harness reported, and each gate row is the gate's own refusal line — not a paraphrase.

Two of these cases had to be rewritten once, and the rewrite belongs in the record because it is the part of a mutation campaign that can quietly lie. Both first reported a compile error rather than a test failure: one deleted a match arm wholesale (`error[E0004]: non-exhaustive patterns`), the other emitted Rust that cannot type-check (`error[E0282]`). A mutation that does not compile proves nothing about behaviour — it shows the anchor was real, not that the guard would have caught the regression. Both were replaced with one-token semantic changes that keep the crate building (returning a different `Denial` from the same arm; matching the wrong `Held` variant), and the driver now labels a non-compiling mutation `MUTATION DID NOT COMPILE` and counts it as a failure of the campaign rather than a pass. The two frontend rows' failing-test names were captured on a targeted re-run of just those cases after the driver's report line was improved; their verdicts came from the full 22-case pass.

The TS lane carries the same duty from the other side: `exposesForbiddenMember` is asserted against a fixture that *does* expose `installUpdate`, and the `BridgedUpdateService` suite drives a fake bridge that can answer with a valid `ready`, an unadmitted proof, another client's identity, install progress, a snapshot with a ride-along key and a transport failure, asserting the service caches, refuses or faults exactly as declared. Because that backend is fake, every `ready` in those tests is contract behaviour against a simulated authority — it is not, and must not be read as, evidence that a real artifact was verified.

## Acceptance-map self-audit (U1–U20)

Each property in `.engineering/prebuilt/HCODER-WO-0027-ACCEPTANCE-SECURITY-MAP.md` was walked against the named proof column, and the audit found one real gap: **U6 had no test that the signed-version refusals map into the bounded vocabulary** — `download_refusal()` classified `MissingSignedVersion` and `SignedVersionMismatch`, but nothing asserted it, so the CRITICAL binding property rested on reading the match arms. `upstream_faults_map_to_bounded_denials_and_never_forward_text` closes that, and doubles as the U18 test that a hostile endpoint string cannot be echoed. The table records the state after that fix.

| U | Proof in this tree | Verdict |
| --- | --- | --- |
| U1 Rust-only | exact `tauri-plugin-updater = "=2.12.0"` in `Cargo.toml`; gate forbids `@tauri-apps/plugin-updater` in frontend source and any `updater` capability permission (`GUEST_UPDATER_PERMISSIONS=0`) | PROVEN_LOCALLY |
| U2 Single bridge | `FRONTEND_INVOKES=6`, all in `desktopBridge.ts`, against the exact allowlist | PROVEN_LOCALLY |
| U3 Argument-free | `UPDATE_COMMAND_ARGS=0` plus `ARGUMENT_FREE_COMMANDS` signature scan; nothing at the Rust entry points accepts a payload | PROVEN_LOCALLY |
| U4 No placeholder trust | `shipped_configuration_is_the_fail_closed_delta_001_node`, `absent_updater_node_is_a_malformed_configuration`, `probe_rejects_unknown_missing_and_alias_ed_keys`; blank-key and empty-endpoint arms reach `service_unavailable` before any request in `unsafe_or_relaxed_controls_are_refused_as_unavailable` | PROVEN_LOCALLY |
| U5 HTTPS only | `only_https_origin_endpoints_are_trusted`, `unsafe_or_relaxed_controls_are_refused_as_unavailable`; `dangerous*` pinned false in config **and** probe | PROVEN_LOCALLY |
| U6 Signed-version binding | `requireSignedVersion: true` written and gate-enforced both ways; `MIN_UPDATER_PLUGIN_VERSION=(2,12,0)` because 2.11.x parses the key away, and now `MIN_TAURI_CLI_VERSION=(2,11,5)` because 2.11.4 never writes the field; two `const _: fn(&Config) -> bool` field-surface pins; refusal mapping covered | PARTLY — the binding is now **satisfiable** by this toolchain and the refusal is proven; a *successful* verified download remains undemonstrable, because the trust data is empty by design and `createUpdaterArtifacts` is forbidden by stop-condition. See the gap section |
| U7 No downgrade | `allowDowngrades: false` in config and probe; `eligibility_is_same_channel_and_strictly_newer_only`, `prerelease_ordering_cannot_be_bypassed_by_string_length`, `version_profile_matches_the_shared_parity_vectors` (DEC-028) | PROVEN_LOCALLY for the Hive law; upstream comparator unexercisable, same reason as U6 |
| U8 Same channel | `channel_is_derived_from_the_installed_version_shape`, cross-channel arm of the eligibility test | PROVEN_LOCALLY |
| U9 Strictly newer | equal/older arms of `eligibility_is_same_channel_and_strictly_newer_only` | PROVEN_LOCALLY |
| U10 Signature before ready | `admitted_without_handle_and_ready_without_download_both_refuse`, `proof_binds_artifact_and_metadata_without_exposing_trust_data`; `PROOF_GATED_TRANSITIONS` restricts `verifying → ready` to an admitted proof | PROVEN_LOCALLY |
| U11 Candidate identity binding | `candidate_identity_substitution_is_refused`, `completion_only_binds_the_candidate_the_slot_owns`, `metadata_identity_is_deterministic_and_binds_every_input`; the `Update` handle is stored only beside the identity it was admitted for | PROVEN_LOCALLY |
| U12 Verified-bytes custody | `pending_slot_holds_exactly_one_candidate`; `verified_candidate_retains_exactly_the_bytes_it_hashed`; `ready_status_carries_a_binding_and_never_the_payload`; `a_second_completion_cannot_replace_the_ready_candidates_bytes`; `FILESYSTEM_MUTATION_PRIMITIVES=0`; no staging path exists. Bytes live in the one `Held::Verified` slot, bound to the `metadata_sha256` their proof hashes, with no file write and no wire field | PROVEN_LOCALLY |
| U13 Proof non-authority | `the_bridge_never_asserts_install_progress`; TS "never lets install progress enter product state, whatever the backend claims"; `install_authority_not_governed` refuses before the proof gate | PROVEN_LOCALLY |
| U14 No install/restart invoke | 6-command allowlist, `INSTALL_RESTART_AUTHORITY=0`, `PRODUCTION_RUST_INSTALL_PRIMITIVES` and `FRONTEND_FORBIDDEN_UPDATE_TEXT` scans; mutation case in the non-vacuity table | PROVEN_LOCALLY |
| U15 No generic network surface | `status_wire_carries_exactly_the_contract_key_set` + `wire_keys` (closed `WireStatus`, no `url`/`signature`/header field exists to arrive in); no generic HTTP plugin; argument-free commands | PROVEN_LOCALLY |
| U16 Package law preserved | `bundle.active: false` unchanged and gate-checked; the six-target matrix itself is a hosted claim | PARTLY — needs Native Package Matrix on the exact head |
| U17 Existing authority preserved | `capabilities/desktop-read-only.json` byte-identical with `permissions: []`; `CAPABILITY_PERMISSIONS=0`; gate green | PROVEN_LOCALLY |
| U18 Error redaction | `Denial { code: &'static str, detail: &'static str }` makes echoing unrepresentable; `every_refusal_uses_the_closed_vocabulary_and_safe_detail`; the new fault test asserts a hostile URL never appears in a detail | PROVEN_LOCALLY |
| U19 Platform truth | `expected_platform()` (updater `Update::target` shape) kept deliberately distinct from `platform_binding()` (`{os}-{arch}`); no arm reports install or restart success on any platform | PROVEN_LOCALLY |
| U20 Exact-head evidence | four workflows green on one SHA | NOT SATISFIED LOCALLY — only hosted runs can close it, and they measure the pushed head, not this commit's ancestors |

Nothing in the right-hand column is a PASS for a property the map marks CRITICAL in a live sense: U6, U16 and U20 are stated as partial or unsatisfied, because no trust material exists, no artifact production is authorized, and hosted evidence can only be measured by a runner. Per the map's closing line, no finding above is closed by prose — every PROVEN_LOCALLY row names a test function or a gate counter that the non-vacuity campaign shows can fail.

## Claims explicitly unavailable at prebuild

No real update check, real signed download, install, restart, rollback, publication or N→N+1 evidence exists yet.

**And still unavailable at this head:** the same list, unchanged in kind. This slice produced the boundary, the trust posture, the law and the proofs that the guards bite, and it removed the toolchain blocker that made the admitted posture unsatisfiable. It did not produce a verified update. What stands between this tree and one is not a version pin any more: it is real trust material (a public key and an endpoint), a signing key held by a publisher, and `createUpdaterArtifacts` enabled by a governed slice — none of which this Work Order may touch.

## Proposed checkpoint delta

Nothing is minted here; this is the text a reviewer can turn into a delta, and no `HCODER-CP-*` number is claimed by this Work Order.

1. **`DEC-031` stays PROPOSED.** Its technical content is implemented and locally proven, and its loadable premise — a signed version a real release can carry — is now satisfiable by the toolchain this tree pins. It is still not *met*, because meeting it needs an artifact that this slice is forbidden to produce. Promotion therefore needs the next two items plus a genuine N→N+1 proof, not a greener local run.
2. **Trust material and artifact production, as a governed decision — not a lock delta.** The previously listed unlock ("a Context Lock delta admitting `package.json` and `package-lock.json` to move the CLI pin") is **withdrawn**: Delta-002 already admits both files conditionally, the condition was met, and the move is done in this branch. What no existing delta admits is a public key, an endpoint, a signing credential or `createUpdaterArtifacts`, and those are exactly what the next slice needs in order to turn `requireSignedVersion: true` from a satisfied requirement into an observed one.
3. **An `HCODER-DIST-001E` scope statement** that re-adds `ready → installing` and `installing → success` to `PROOF_GATED_TRANSITIONS` at the same moment it introduces install authority, and that keeps `bundle.active: false` until then. The obligation already sits in the code comment; it belongs in the governance record too, because a slice that adds install without re-adding those two edges would leave `ready` one step from a mutation with nothing between them.
4. **No hosted-evidence claim may be back-filled into this file.** When this branch is pushed, the four hosted rows are re-measured against the new head and the old rows are superseded, not edited — a head move invalidates every receipt above it.
